apiVersion: v1
kind: Service
metadata:
  name: identity
  namespace: saas-platform
  labels:
    app: identity
spec:
  type: ClusterIP
  selector:
    app: identity
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
